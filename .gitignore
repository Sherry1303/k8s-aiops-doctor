# ---------------------------------------------------------------------------
# Python 运行期产物
# ---------------------------------------------------------------------------
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
build/
dist/
.venv/
venv/
env/

# ---------------------------------------------------------------------------
# 密钥与本机配置（绝对不能提交）
# ---------------------------------------------------------------------------
.env
.env.*
!.env.example

# ---------------------------------------------------------------------------
# 自愈模式(--heal)产生的 Pod 清单：属于运行期产物，重新跑一次即可生成
#   *-original.yaml 是被修复前的原始备份（按要求不入库）
#   *-healed.yaml   是注入修复镜像后的清单（同样是产物，不入库）
# 目录本身会在自愈时自动创建，repo 中不保留空目录
# ---------------------------------------------------------------------------
heal-manifests/*-original.yaml
heal-manifests/*-healed.yaml

# ---------------------------------------------------------------------------
# 邮件 HTML 报告落盘目录（--email / --email-dry-run 生成，可随时复现）
# ---------------------------------------------------------------------------
reports/

# ---------------------------------------------------------------------------
# 临时调试文件与日志
# ---------------------------------------------------------------------------
*.log
_*.txt
_*.err
*.tmp

# ---------------------------------------------------------------------------
# 编辑器 / 操作系统噪音
# ---------------------------------------------------------------------------
.idea/
.vscode/
.DS_Store
Thumbs.db
desktop.ini
